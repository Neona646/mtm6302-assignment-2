const gallary = document.getElementById('gallary')

const $image = document.querySelector('img')

const empty = document.querySelector


$images.forEach(img =>{
  img.addEventListener('click', () => {
    img.dataset = innerHTML = `
    <h2> Characters </h2>`
  })
});


$image.addEventListener('mouseover' , function(e){
  console.log($image.dataset.name)
   
  //  console.log(image.dataset.imageType) // Two dataset words into one. 
  //  console.log(image.dataset.name) // one dataset word
})